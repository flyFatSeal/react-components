import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import RealtimeCurve from '../index';

describe('RealtimeCurve', () => {
   it('renders without error', () => {

   });
});
